data_preprocess:
  test_size : 0.4
  random_state: 30

build_features:
  max_features: 1000

model_building:
  n_estimators: 500
  learning_rate: 0.2

model_evaluation:
  model_path : models/model.pkl
  test_data_path: data/feature/test_tfidf.csv
  metrics_path: metrics.json
